---
import Breadcrumbs from '@/components/Breadcrumbs.astro'
import Link from '@/components/Link.astro'
import PageHead from '@/components/PageHead.astro'
import ProjectCard from '@/components/ProjectCard.astro'
import Layout from '@/layouts/Layout.astro'
import { getAllProjects } from '@/lib/data-utils'
import { Icon } from 'astro-icon/components'
import ContactInfo from '@/components/ContactInfo.astro'
import { SOCIAL_LINKS } from '@/consts'
import { Button } from '@/components/ui/button'

const projects = await getAllProjects()
---

<Layout class="max-w-3xl">
  <PageHead slot="head" title="About" />
  <Breadcrumbs items={[{ label: 'About', icon: 'lucide:info' }]} />

  <section>
    <div class="min-w-full">
      <div class="mb-8 sm:flex sm:gap-12">
        <div class="prose w-full">
          <span class="text-4xl font-mono font-bold uppercase pb-0 sm:mb-0 sm:pt-4 sm:pr-3 sm:inline-block sm:float-left">
            Julian
            <span class="text-primary">C</span>
            Sy
          </span>
          <p class="text-sm mt-2 sm:mt-4">is an aspiring <strong>UX Developer</strong> currently pursuing a BS UXD at Western Governors University. The foundation for his personal and professional pursuits alike is the search for <strong>understanding</strong>.</p>
          <p class="text-sm">He wants to understand <strong>people</strong>, whether it's identifying and reducing friction users might encounter or improvising a new subplot when he's running a tabletop RPG. He seeks to know how things actually work in <strong>practice</strong>, programming at work and play to make his own ideas come to life. And while it's important to understand others and how things work, the real <strong>progress</strong> happens when you seek to understand yourself.
          </p>
        </div>
        <div class="mt-6 sm:mt-0">
          <ContactInfo links={SOCIAL_LINKS} />
          <Button
            variant='secondary'
            className="mt-3 mx-auto uppercase block sm:w-full"
          >
            <Link href="#">
              <Icon
                name='lucide:download'
                class="size-4 inline-block mr-1.5"
              />
              PDF Resume
            </Link>
          </Button>
        </div>
      </div>

      <!-- <h2 class="mb-4 text-2xl font-medium">Example Projects Listing</h2>
      <div class="flex flex-col gap-4">
        {projects.map((project) => <ProjectCard project={project} />)}
      </div> -->
    </div>
  </section>
</Layout>
